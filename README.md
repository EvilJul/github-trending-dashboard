# GitHub Trending Projects Dashboard

> 每周精选最热门的开源项目

![Dashboard Preview](docs/images/preview.png)

## ✨ 特性

- 🚀 **前后端分离架构** - FastAPI + 现代Web前端
- 📊 **实时数据获取** - 从GitHub API自动抓取热门项目
- 🎨 **优雅的界面** - 深色主题、流畅过渡动效
- 📱 **响应式设计** - 完美适配桌面和移动设备
- 📋 **历史记录** - 查看过往的热门项目报告
- 🔄 **自动刷新** - 每周五上午10:00自动更新
- 📚 **完整文档** - API文档、部署指南
- 🤖 **AI 增强** - 使用大模型生成更丰富的项目描述

## 🏗️ 架构

```
┌─────────────────────────────────────────────────────────┐
│                      Frontend                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │
│  │ index.html  │  │ history.html│  │  project.html   │  │
│  └─────────────┘  └─────────────┘  └─────────────────┘  │
└────────────────────────┬────────────────────────────────┘
                         │ HTTP API
┌────────────────────────┴────────────────────────────────┐
│                      Backend (FastAPI)                   │
│  ┌──────────────────────────────────────────────────┐   │
│  │  /api/projects  │  /api/refresh  │  /api/history  │   │
│  │  /api/readme    │  /api/refresh-ai               │   │
│  └──────────────────────────────────────────────────┘   │
│                          │                               │
│              ┌───────────┴───────────┐                   │
│              │   GitHub Scraper      │                   │
│              └───────────────────────┘                   │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────┐
│                       Data Storage                       │
│        data/projects.json  │  data/history.json         │
└─────────────────────────────────────────────────────────┘
```

## 🚀 快速开始

### 1. 安装依赖

```bash
cd backend
pip install -r requirements.txt
```

### 2. 启动后端服务

```bash
# 开发模式
python main.py --port 8001

# 或使用 uvicorn
uvicorn main:app --reload --host 0.0.0.0 --port 8001
```

### 3. 访问应用

打开浏览器访问: http://localhost:8001

## 📖 使用指南

### 获取最新项目

1. 点击右上角「刷新数据」按钮
2. 等待后端从GitHub获取数据
3. 数据自动保存并显示

### AI 增强刷新

使用 AI 模型生成更丰富的项目描述和使用指南：

1. 先点击「API配置」配置你的 AI 服务（如通义千问、OpenAI）
2. 点击「AI 增强」按钮
3. AI 会为每个项目生成详细描述和使用步骤

### 筛选项目

使用语言筛选按钮快速找到感兴趣的项目：
- ☕ Java
- 🐍 Python
- 📜 JavaScript
- 📘 TypeScript

### 查看项目详情

点击任意项目卡片，可查看：
- 项目详细信息
- AI 增强的使用指南
- 原始 README 内容（Markdown 渲染）

### 查看历史

点击顶部「历史记录」查看过往报告。

## ⚙️ 配置说明

### 配置文件

项目根目录下的 `config.json` 包含所有配置：

```json
{
  "settings": {
    "updateSchedule": "0 10 * * 5",
    "languages": ["Python", "JavaScript", "TypeScript", "Go", "Java"],
    "numberOfProjects": 10
  },
  "ai": {
    "provider": "qwen",
    "model": "qwen-plus",
    "endpoint": "",
    "apiKey": ""
  }
}
```

> ⚠️ `config.json` 包含敏感信息，请勿提交到版本控制
> 参考 `config.example.json` 查看配置模板

### 定时任务 (可选)

每周五上午10:00自动刷新数据（使用系统 crontab）：

```bash
# 添加定时任务
crontab -e

# 添加以下行：
# 0 10 * * 5 curl -X POST http://localhost:8001/api/projects/refresh
```

### 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| PORT | 服务端口 | 8001 |
| HOST | 绑定地址 | 0.0.0.0 |

## 📚 API 文档

启动服务后访问：
- **Swagger UI**: http://localhost:8001/docs
- **ReDoc**: http://localhost:8001/redoc

### 主要端点

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/projects/` | 获取项目列表 |
| POST | `/api/projects/refresh` | 刷新数据 |
| POST | `/api/projects/refresh-ai` | AI 增强刷新 |
| GET | `/api/projects/{name}` | 获取单个项目 |
| GET | `/api/projects/{name}/readme` | 获取 README |
| GET | `/api/projects/stats/summary` | 获取统计信息 |
| GET | `/api/history/` | 获取历史记录 |
| GET | `/api/history/{id}` | 获取单条历史记录 |
| GET | `/health` | 健康检查 |

## 📁 项目结构

```
github_trending_projects/
├── backend/
│   ├── main.py              # FastAPI 主服务
│   ├── requirements.txt     # Python 依赖
│   ├── routers/
│   │   ├── projects.py      # 项目相关 API
│   │   └── history.py       # 历史记录 API
│   ├── services/
│   │   ├── github.py        # GitHub 数据获取
│   │   ├── storage.py       # 数据存储
│   │   └── ai.py            # AI 增强服务
│   ├── models/
│   │   └── schemas.py       # Pydantic 模型
│   └── data/                # 数据文件存储
├── web/
│   ├── index.html           # 主页面
│   ├── history.html         # 历史记录页
│   ├── project.html         # 项目详情页
│   ├── report.html          # 报告详情页
│   └── dashboard.js         # 前端逻辑
├── data/                    # 公共数据目录
├── config.json              # 配置文件（敏感）
├── config.example.json      # 配置示例
├── docs/                    # 文档
└── README.md                # 本文件
```

## 🛠️ 开发

### 添加新功能

1. **后端**: 在 `backend/routers/` 添加新路由
2. **前端**: 在 `web/dashboard.js` 添加新方法
3. **样式**: 在对应 HTML 的 `<style>` 中添加

### 测试 API

```bash
# 健康检查
curl http://localhost:8001/health

# 获取项目列表
curl http://localhost:8001/api/projects/

# 刷新数据
curl -X POST http://localhost:8001/api/projects/refresh

# 获取 README
curl http://localhost:8001/api/projects/skills/readme
```

## 📝 更新日志

### v1.2.0 (2026-02-03)
- ✨ 新增 AI 增强功能（使用大模型生成项目描述）
- 📦 新增项目详情页（project.html）
- 📄 README Markdown 渲染（使用 marked.js）
- 🔗 优化项目卡片点击体验（整卡可点击）
- 📊 新增 `/api/projects/{name}/readme` 接口
- 🔄 新增 `/api/projects/refresh-ai` AI 增强刷新接口
- 🛡️ 配置文件安全优化（分离敏感信息）

### v1.1.0 (2026-02-03)
- ✨ 新增前后端分离架构
- 🎨 优化界面动效 (页面切换、卡片入场动画)
- 📊 新增加载状态动画
- 📚 完善API文档
- 🔄 支持实时数据刷新
- 📋 完善历史记录功能

### v1.0.0 (2026-01-29)
- 🎉 初始版本
- 📊 项目展示功能
- 🌙 深色主题

## 📄 许可证

MIT License

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
